---
description: Check current Azure spend against the $100 budget
---

Use the cost-checker subagent to produce a current Azure spend report.

If `az` CLI is not authenticated, ask me to run `az login` first.
